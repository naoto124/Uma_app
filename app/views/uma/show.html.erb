<h1><%= @keyword%>行</h1>
  <ul>
    <% @uma.each do |u| %>
      <li><a href="uma_detail/<%=u.name%>" name="<%= u.name%>"><%= u.name%></a></li>

    <% end %>
  </ul>
<div class="paginate">
  <%= paginate @uma %>
</div>